# regkit 0.3.0
* Package renamed from `regtools` to `regkit`
* Renamed function `synthetic_data()` to `simulate_data()`
* Added Dependencies article in documentation
* Improved/clarified the documentation and examples based on comments from JOSS review 


# regtools 0.2.0

* Added a `NEWS.md` file to track changes to the package.
* Added `plot_map()` function to create Norwegian choropleth municipality and country maps 
* Added extra check in `synthetic_data()`, due to some incompatibility issues with KlassR it currently requires stringr v1.5.1 
